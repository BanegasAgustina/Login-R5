# En desarrollo se recomienda el proxy de Vite para /api.
# Solo una URL pública puede llevar prefijo VITE_; nunca Client Secrets.
VITE_API_URL=/api

# En Vercel con proyectos separados, configurar la URL pública de la API con /api.
# Las cookies OAuth requieren frontend/API HTTPS del mismo sitio.
